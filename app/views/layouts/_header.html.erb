<nav class="uk-light uk-background-primary" uk-navbar>
  <div class="uk-navbar-left">
    <a class="uk-navbar-item uk-logo" href="#">Prophetter</a>
  </div>
  <div class="uk-navbar-right">
    <ul class="uk-navbar-nav">
      <li class="uk-active">
        <% if user_signed_in?  %>
          <%= image_tag current_user.image_url %>
        <% else %>
          <%= link_to 'Twitterでログイン', "/auth/twitter" %>
        <% end %>
      </li>
    </ul>
  </div>
</nav>
